Limit  (cost=190.72..200.38 rows=84 width=2313) (actual time=0.675..0.732 rows=52 loops=1)
  Output: id, original_id, name, price, category, image, slug, category_slug, short_description, description, key_features, features, images, is_best_seller, is_custom_request, custom_form_config, default_form_fields, variants, created_at, is_gift_bundle_item, is_live, hub, is_affiliate_eligible, affiliate_commission_rate, affiliate_payout_type, affiliate_fixed_amount, min_affiliate_level, avg_rating, reviews_count, gst_rate, normalized_name, normalized_category, normalized_description, (CASE WHEN (normalized_name ~~ '%coffee mug%'::text) THEN 'coffee mug'::text WHEN (normalized_name ~~ '%tea cup%'::text) THEN 'tea cup'::text WHEN (normalized_name ~~ '%gift hamper%'::text) THEN 'gift hamper'::text WHEN (normalized_name ~~ '%soap holder%'::text) THEN 'soap holder'::text WHEN (normalized_name ~~ '%soap%'::text) THEN 'soap'::text WHEN (normalized_name ~~ '%necklace%'::text) THEN 'necklace'::text WHEN (normalized_name ~~ '%planter%'::text) THEN 'planter'::text WHEN (normalized_name ~~ '%mug%'::text) THEN 'mug'::text WHEN (normalized_name ~~ '%cup%'::text) THEN 'cup'::text ELSE 'other'::text END), (((CASE WHEN (normalized_name = 'coconut shell'::text) THEN 1000 WHEN (normalized_name ~~ 'coconut shell%'::text) THEN 900 WHEN ((POSITION(('coconut'::text) IN (normalized_name)) > 0) AND (POSITION(('shell'::text) IN (normalized_name)) > 0)) THEN 800 WHEN (normalized_name ~~ '%coconut shell%'::text) THEN 700 WHEN ((POSITION(('coconut'::text) IN (normalized_category)) > 0) AND (POSITION(('shell'::text) IN (normalized_category)) > 0)) THEN 300 WHEN ((POSITION(('coconut'::text) IN (normalized_description)) > 0) AND (POSITION(('shell'::text) IN (normalized_description)) > 0)) THEN 100 ELSE 0 END + 0) + 0)), (CASE WHEN (normalized_name = 'coconut shell'::text) THEN 'Exact Name'::text WHEN (normalized_name ~~ 'coconut shell%'::text) THEN 'Starts With Name'::text WHEN ((POSITION(('coconut'::text) IN (normalized_name)) > 0) AND (POSITION(('shell'::text) IN (normalized_name)) > 0)) THEN 'All Tokens In Name'::text WHEN (normalized_name ~~ '%coconut shell%'::text) THEN 'Contains Name'::text WHEN ((POSITION(('coconut'::text) IN (normalized_category)) > 0) AND (POSITION(('shell'::text) IN (normalized_category)) > 0)) THEN 'Category'::text WHEN ((POSITION(('coconut'::text) IN (normalized_description)) > 0) AND (POSITION(('shell'::text) IN (normalized_description)) > 0)) THEN 'Description'::text ELSE 'Other'::text END)
  Buffers: shared hit=175
  ->  Result  (cost=190.72..200.38 rows=84 width=2313) (actual time=0.674..0.726 rows=52 loops=1)
        Output: id, original_id, name, price, category, image, slug, category_slug, short_description, description, key_features, features, images, is_best_seller, is_custom_request, custom_form_config, default_form_fields, variants, created_at, is_gift_bundle_item, is_live, hub, is_affiliate_eligible, affiliate_commission_rate, affiliate_payout_type, affiliate_fixed_amount, min_affiliate_level, avg_rating, reviews_count, gst_rate, normalized_name, normalized_category, normalized_description, (CASE WHEN (normalized_name ~~ '%coffee mug%'::text) THEN 'coffee mug'::text WHEN (normalized_name ~~ '%tea cup%'::text) THEN 'tea cup'::text WHEN (normalized_name ~~ '%gift hamper%'::text) THEN 'gift hamper'::text WHEN (normalized_name ~~ '%soap holder%'::text) THEN 'soap holder'::text WHEN (normalized_name ~~ '%soap%'::text) THEN 'soap'::text WHEN (normalized_name ~~ '%necklace%'::text) THEN 'necklace'::text WHEN (normalized_name ~~ '%planter%'::text) THEN 'planter'::text WHEN (normalized_name ~~ '%mug%'::text) THEN 'mug'::text WHEN (normalized_name ~~ '%cup%'::text) THEN 'cup'::text ELSE 'other'::text END), (((CASE WHEN (normalized_name = 'coconut shell'::text) THEN 1000 WHEN (normalized_name ~~ 'coconut shell%'::text) THEN 900 WHEN ((POSITION(('coconut'::text) IN (normalized_name)) > 0) AND (POSITION(('shell'::text) IN (normalized_name)) > 0)) THEN 800 WHEN (normalized_name ~~ '%coconut shell%'::text) THEN 700 WHEN ((POSITION(('coconut'::text) IN (normalized_category)) > 0) AND (POSITION(('shell'::text) IN (normalized_category)) > 0)) THEN 300 WHEN ((POSITION(('coconut'::text) IN (normalized_description)) > 0) AND (POSITION(('shell'::text) IN (normalized_description)) > 0)) THEN 100 ELSE 0 END + 0) + 0)), CASE WHEN (normalized_name = 'coconut shell'::text) THEN 'Exact Name'::text WHEN (normalized_name ~~ 'coconut shell%'::text) THEN 'Starts With Name'::text WHEN ((POSITION(('coconut'::text) IN (normalized_name)) > 0) AND (POSITION(('shell'::text) IN (normalized_name)) > 0)) THEN 'All Tokens In Name'::text WHEN (normalized_name ~~ '%coconut shell%'::text) THEN 'Contains Name'::text WHEN ((POSITION(('coconut'::text) IN (normalized_category)) > 0) AND (POSITION(('shell'::text) IN (normalized_category)) > 0)) THEN 'Category'::text WHEN ((POSITION(('coconut'::text) IN (normalized_description)) > 0) AND (POSITION(('shell'::text) IN (normalized_description)) > 0)) THEN 'Description'::text ELSE 'Other'::text END
        Buffers: shared hit=175
        ->  Sort  (cost=190.72..190.93 rows=84 width=2281) (actual time=0.670..0.674 rows=52 loops=1)
              Output: id, original_id, name, price, category, image, slug, category_slug, short_description, description, key_features, features, images, is_best_seller, is_custom_request, custom_form_config, default_form_fields, variants, created_at, is_gift_bundle_item, is_live, hub, is_affiliate_eligible, affiliate_commission_rate, affiliate_payout_type, affiliate_fixed_amount, min_affiliate_level, avg_rating, reviews_count, gst_rate, normalized_name, normalized_category, normalized_description, (CASE WHEN (normalized_name ~~ '%coffee mug%'::text) THEN 'coffee mug'::text WHEN (normalized_name ~~ '%tea cup%'::text) THEN 'tea cup'::text WHEN (normalized_name ~~ '%gift hamper%'::text) THEN 'gift hamper'::text WHEN (normalized_name ~~ '%soap holder%'::text) THEN 'soap holder'::text WHEN (normalized_name ~~ '%soap%'::text) THEN 'soap'::text WHEN (normalized_name ~~ '%necklace%'::text) THEN 'necklace'::text WHEN (normalized_name ~~ '%planter%'::text) THEN 'planter'::text WHEN (normalized_name ~~ '%mug%'::text) THEN 'mug'::text WHEN (normalized_name ~~ '%cup%'::text) THEN 'cup'::text ELSE 'other'::text END), (((CASE WHEN (normalized_name = 'coconut shell'::text) THEN 1000 WHEN (normalized_name ~~ 'coconut shell%'::text) THEN 900 WHEN ((POSITION(('coconut'::text) IN (normalized_name)) > 0) AND (POSITION(('shell'::text) IN (normalized_name)) > 0)) THEN 800 WHEN (normalized_name ~~ '%coconut shell%'::text) THEN 700 WHEN ((POSITION(('coconut'::text) IN (normalized_category)) > 0) AND (POSITION(('shell'::text) IN (normalized_category)) > 0)) THEN 300 WHEN ((POSITION(('coconut'::text) IN (normalized_description)) > 0) AND (POSITION(('shell'::text) IN (normalized_description)) > 0)) THEN 100 ELSE 0 END + 0) + 0))
              Sort Key: (((CASE WHEN (products.normalized_name = 'coconut shell'::text) THEN 1000 WHEN (products.normalized_name ~~ 'coconut shell%'::text) THEN 900 WHEN ((POSITION(('coconut'::text) IN (products.normalized_name)) > 0) AND (POSITION(('shell'::text) IN (products.normalized_name)) > 0)) THEN 800 WHEN (products.normalized_name ~~ '%coconut shell%'::text) THEN 700 WHEN ((POSITION(('coconut'::text) IN (products.normalized_category)) > 0) AND (POSITION(('shell'::text) IN (products.normalized_category)) > 0)) THEN 300 WHEN ((POSITION(('coconut'::text) IN (products.normalized_description)) > 0) AND (POSITION(('shell'::text) IN (products.normalized_description)) > 0)) THEN 100 ELSE 0 END + 0) + 0)) DESC, products.created_at DESC
              Sort Method: quicksort  Memory: 129kB
              Buffers: shared hit=175
              ->  Seq Scan on public.products  (cost=0.00..188.03 rows=84 width=2281) (actual time=0.027..0.512 rows=52 loops=1)
                    Output: id, original_id, name, price, category, image, slug, category_slug, short_description, description, key_features, features, images, is_best_seller, is_custom_request, custom_form_config, default_form_fields, variants, created_at, is_gift_bundle_item, is_live, hub, is_affiliate_eligible, affiliate_commission_rate, affiliate_payout_type, affiliate_fixed_amount, min_affiliate_level, avg_rating, reviews_count, gst_rate, normalized_name, normalized_category, normalized_description, CASE WHEN (normalized_name ~~ '%coffee mug%'::text) THEN 'coffee mug'::text WHEN (normalized_name ~~ '%tea cup%'::text) THEN 'tea cup'::text WHEN (normalized_name ~~ '%gift hamper%'::text) THEN 'gift hamper'::text WHEN (normalized_name ~~ '%soap holder%'::text) THEN 'soap holder'::text WHEN (normalized_name ~~ '%soap%'::text) THEN 'soap'::text WHEN (normalized_name ~~ '%necklace%'::text) THEN 'necklace'::text WHEN (normalized_name ~~ '%planter%'::text) THEN 'planter'::text WHEN (normalized_name ~~ '%mug%'::text) THEN 'mug'::text WHEN (normalized_name ~~ '%cup%'::text) THEN 'cup'::text ELSE 'other'::text END, ((CASE WHEN (normalized_name = 'coconut shell'::text) THEN 1000 WHEN (normalized_name ~~ 'coconut shell%'::text) THEN 900 WHEN ((POSITION(('coconut'::text) IN (normalized_name)) > 0) AND (POSITION(('shell'::text) IN (normalized_name)) > 0)) THEN 800 WHEN (normalized_name ~~ '%coconut shell%'::text) THEN 700 WHEN ((POSITION(('coconut'::text) IN (normalized_category)) > 0) AND (POSITION(('shell'::text) IN (normalized_category)) > 0)) THEN 300 WHEN ((POSITION(('coconut'::text) IN (normalized_description)) > 0) AND (POSITION(('shell'::text) IN (normalized_description)) > 0)) THEN 100 ELSE 0 END + 0) + 0)
                    Filter: (products.is_live AND ((products.normalized_name = 'coconut shell'::text) OR (products.normalized_name ~~ 'coconut shell%'::text) OR ((POSITION(('coconut'::text) IN (products.normalized_name)) > 0) AND (POSITION(('shell'::text) IN (products.normalized_name)) > 0)) OR (products.normalized_name ~~ '%coconut shell%'::text) OR ((POSITION(('coconut'::text) IN (products.normalized_category)) > 0) AND (POSITION(('shell'::text) IN (products.normalized_category)) > 0))))
                    Rows Removed by Filter: 150
                    Buffers: shared hit=175
Query Identifier: -2708123083030277354
Planning:
  Buffers: shared hit=2
Planning Time: 0.324 ms
Execution Time: 0.832 ms