Limit  (cost=194.31..194.63 rows=128 width=2313) (actual time=0.600..0.606 rows=36 loops=1)
  Output: id, original_id, name, price, category, image, slug, category_slug, short_description, description, key_features, features, images, is_best_seller, is_custom_request, custom_form_config, default_form_fields, variants, created_at, is_gift_bundle_item, is_live, hub, is_affiliate_eligible, affiliate_commission_rate, affiliate_payout_type, affiliate_fixed_amount, min_affiliate_level, avg_rating, reviews_count, gst_rate, normalized_name, normalized_category, normalized_description, (CASE WHEN (normalized_name ~~ '%coffee mug%'::text) THEN 'coffee mug'::text WHEN (normalized_name ~~ '%tea cup%'::text) THEN 'tea cup'::text WHEN (normalized_name ~~ '%gift hamper%'::text) THEN 'gift hamper'::text WHEN (normalized_name ~~ '%soap holder%'::text) THEN 'soap holder'::text WHEN (normalized_name ~~ '%soap%'::text) THEN 'soap'::text WHEN (normalized_name ~~ '%necklace%'::text) THEN 'necklace'::text WHEN (normalized_name ~~ '%planter%'::text) THEN 'planter'::text WHEN (normalized_name ~~ '%mug%'::text) THEN 'mug'::text WHEN (normalized_name ~~ '%cup%'::text) THEN 'cup'::text ELSE 'other'::text END), (((CASE WHEN (normalized_name = 'terracotta'::text) THEN 1000 WHEN (normalized_name ~~ 'terracotta%'::text) THEN 900 WHEN (POSITION(('terracotta'::text) IN (normalized_name)) > 0) THEN 800 WHEN (normalized_name ~~ '%terracotta%'::text) THEN 700 WHEN (POSITION(('terracotta'::text) IN (normalized_category)) > 0) THEN 300 WHEN (POSITION(('terracotta'::text) IN (normalized_description)) > 0) THEN 100 ELSE 0 END + 0) + 0)), (CASE WHEN (normalized_name = 'terracotta'::text) THEN 'Exact Name'::text WHEN (normalized_name ~~ 'terracotta%'::text) THEN 'Starts With Name'::text WHEN (POSITION(('terracotta'::text) IN (normalized_name)) > 0) THEN 'All Tokens In Name'::text WHEN (normalized_name ~~ '%terracotta%'::text) THEN 'Contains Name'::text WHEN (POSITION(('terracotta'::text) IN (normalized_category)) > 0) THEN 'Category'::text WHEN (POSITION(('terracotta'::text) IN (normalized_description)) > 0) THEN 'Description'::text ELSE 'Other'::text END)
  Buffers: shared hit=175
  ->  Sort  (cost=194.31..194.63 rows=128 width=2313) (actual time=0.598..0.601 rows=36 loops=1)
        Output: id, original_id, name, price, category, image, slug, category_slug, short_description, description, key_features, features, images, is_best_seller, is_custom_request, custom_form_config, default_form_fields, variants, created_at, is_gift_bundle_item, is_live, hub, is_affiliate_eligible, affiliate_commission_rate, affiliate_payout_type, affiliate_fixed_amount, min_affiliate_level, avg_rating, reviews_count, gst_rate, normalized_name, normalized_category, normalized_description, (CASE WHEN (normalized_name ~~ '%coffee mug%'::text) THEN 'coffee mug'::text WHEN (normalized_name ~~ '%tea cup%'::text) THEN 'tea cup'::text WHEN (normalized_name ~~ '%gift hamper%'::text) THEN 'gift hamper'::text WHEN (normalized_name ~~ '%soap holder%'::text) THEN 'soap holder'::text WHEN (normalized_name ~~ '%soap%'::text) THEN 'soap'::text WHEN (normalized_name ~~ '%necklace%'::text) THEN 'necklace'::text WHEN (normalized_name ~~ '%planter%'::text) THEN 'planter'::text WHEN (normalized_name ~~ '%mug%'::text) THEN 'mug'::text WHEN (normalized_name ~~ '%cup%'::text) THEN 'cup'::text ELSE 'other'::text END), (((CASE WHEN (normalized_name = 'terracotta'::text) THEN 1000 WHEN (normalized_name ~~ 'terracotta%'::text) THEN 900 WHEN (POSITION(('terracotta'::text) IN (normalized_name)) > 0) THEN 800 WHEN (normalized_name ~~ '%terracotta%'::text) THEN 700 WHEN (POSITION(('terracotta'::text) IN (normalized_category)) > 0) THEN 300 WHEN (POSITION(('terracotta'::text) IN (normalized_description)) > 0) THEN 100 ELSE 0 END + 0) + 0)), (CASE WHEN (normalized_name = 'terracotta'::text) THEN 'Exact Name'::text WHEN (normalized_name ~~ 'terracotta%'::text) THEN 'Starts With Name'::text WHEN (POSITION(('terracotta'::text) IN (normalized_name)) > 0) THEN 'All Tokens In Name'::text WHEN (normalized_name ~~ '%terracotta%'::text) THEN 'Contains Name'::text WHEN (POSITION(('terracotta'::text) IN (normalized_category)) > 0) THEN 'Category'::text WHEN (POSITION(('terracotta'::text) IN (normalized_description)) > 0) THEN 'Description'::text ELSE 'Other'::text END)
        Sort Key: (((CASE WHEN (products.normalized_name = 'terracotta'::text) THEN 1000 WHEN (products.normalized_name ~~ 'terracotta%'::text) THEN 900 WHEN (POSITION(('terracotta'::text) IN (products.normalized_name)) > 0) THEN 800 WHEN (products.normalized_name ~~ '%terracotta%'::text) THEN 700 WHEN (POSITION(('terracotta'::text) IN (products.normalized_category)) > 0) THEN 300 WHEN (POSITION(('terracotta'::text) IN (products.normalized_description)) > 0) THEN 100 ELSE 0 END + 0) + 0)) DESC, products.created_at DESC
        Sort Method: quicksort  Memory: 97kB
        Buffers: shared hit=175
        ->  Seq Scan on public.products  (cost=0.00..189.84 rows=128 width=2313) (actual time=0.029..0.438 rows=36 loops=1)
              Output: id, original_id, name, price, category, image, slug, category_slug, short_description, description, key_features, features, images, is_best_seller, is_custom_request, custom_form_config, default_form_fields, variants, created_at, is_gift_bundle_item, is_live, hub, is_affiliate_eligible, affiliate_commission_rate, affiliate_payout_type, affiliate_fixed_amount, min_affiliate_level, avg_rating, reviews_count, gst_rate, normalized_name, normalized_category, normalized_description, CASE WHEN (normalized_name ~~ '%coffee mug%'::text) THEN 'coffee mug'::text WHEN (normalized_name ~~ '%tea cup%'::text) THEN 'tea cup'::text WHEN (normalized_name ~~ '%gift hamper%'::text) THEN 'gift hamper'::text WHEN (normalized_name ~~ '%soap holder%'::text) THEN 'soap holder'::text WHEN (normalized_name ~~ '%soap%'::text) THEN 'soap'::text WHEN (normalized_name ~~ '%necklace%'::text) THEN 'necklace'::text WHEN (normalized_name ~~ '%planter%'::text) THEN 'planter'::text WHEN (normalized_name ~~ '%mug%'::text) THEN 'mug'::text WHEN (normalized_name ~~ '%cup%'::text) THEN 'cup'::text ELSE 'other'::text END, ((CASE WHEN (normalized_name = 'terracotta'::text) THEN 1000 WHEN (normalized_name ~~ 'terracotta%'::text) THEN 900 WHEN (POSITION(('terracotta'::text) IN (normalized_name)) > 0) THEN 800 WHEN (normalized_name ~~ '%terracotta%'::text) THEN 700 WHEN (POSITION(('terracotta'::text) IN (normalized_category)) > 0) THEN 300 WHEN (POSITION(('terracotta'::text) IN (normalized_description)) > 0) THEN 100 ELSE 0 END + 0) + 0), CASE WHEN (normalized_name = 'terracotta'::text) THEN 'Exact Name'::text WHEN (normalized_name ~~ 'terracotta%'::text) THEN 'Starts With Name'::text WHEN (POSITION(('terracotta'::text) IN (normalized_name)) > 0) THEN 'All Tokens In Name'::text WHEN (normalized_name ~~ '%terracotta%'::text) THEN 'Contains Name'::text WHEN (POSITION(('terracotta'::text) IN (normalized_category)) > 0) THEN 'Category'::text WHEN (POSITION(('terracotta'::text) IN (normalized_description)) > 0) THEN 'Description'::text ELSE 'Other'::text END
              Filter: (products.is_live AND ((products.normalized_name = 'terracotta'::text) OR (products.normalized_name ~~ 'terracotta%'::text) OR (POSITION(('terracotta'::text) IN (products.normalized_name)) > 0) OR (products.normalized_name ~~ '%terracotta%'::text) OR (POSITION(('terracotta'::text) IN (products.normalized_category)) > 0)))
              Rows Removed by Filter: 166
              Buffers: shared hit=175
Query Identifier: -9020480014696723261
Planning:
  Buffers: shared hit=2
Planning Time: 0.267 ms
Execution Time: 0.662 ms